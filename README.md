# Shocking VRC
Bridge gap with DG-LAB and VRC

App is still in development, dev builds are not available yet but you can build yourself by grabbing latest source code from [back branch](https://github.com/nullshii/ShockingVRC/tree/back) and build using
```sh
$env:CARGO_TARGET_DIR = "D:\Dev\rust\ShockingVRC\build"; cargo build --release --example cli 
```
command